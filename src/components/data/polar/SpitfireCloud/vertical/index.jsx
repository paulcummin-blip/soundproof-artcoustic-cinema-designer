// src/components/data/polar/SpitfireCloud/vertical/index.jsx
// Measured vertical-plane PAS/FRD curves for Spitfire Cloud, keyed by measured angle (degrees).
// EMPTY until real measurement files are supplied — do NOT add placeholder/invented data here.
// Shape once populated: { [angleDeg: number]: Array<{ frequency: number, spl: number }> }
// Angles and frequency points are discovered dynamically by the loader — no fixed spacing assumed.
export const verticalDataset = {};